{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "cross-env CHOKIDAR_USEPOLLING=true nuxt dev --port 8085 --host 0.0.0.0",
    "dev2": "cross-env CHOKIDAR_USEPOLLING=true node_modules/nuxt/bin/nuxt.mjs dev --port 8081 --host 0.0.0.0",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "@nuxt/webpack-builder": "^3.1.1",
    "cross-env": "^7.0.3",
    "nuxt": "^3.1.1"
  }
}
